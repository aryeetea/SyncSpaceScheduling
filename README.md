
  # Sync-Space

  This is a code bundle for Sync-Space. The original project is available at https://www.figma.com/design/NwHSAnriGbXUGq13p7bRbu/Sync-Space.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # SyncSpaceScheduling
